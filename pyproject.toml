[project]
name = "housing-scraper"
version = "0.1.0"
description = "Simple Housing Scraper for Mercado Libre Apartments"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pytest-playwright>=0.7.2",
]
